name: CI

on:
  push:
    paths:
      - "src/**"
      - "cpp/**"
      - "docs/**"
  pull_request:
  workflow_dispatch:

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      # Python
      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install dependencies (project + dev)
        run: |
          python -m pip install --upgrade pip
          pip install -e .[dev]

      - name: Run pre-commit (all files)
        run: |
          pre-commit autoupdate --repo https://github.com/pycqa/isort
          pre-commit run --all-files

      - name: Run tests
        env:
          PYTHONPATH: ${{ github.workspace }}
        run: |
          pytest -q --cov --cov-branch --cov-report=xml --cov-fail-under=85

      - name: Upload coverage reports to Codecov
        uses: codecov/codecov-action@v6
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          fail_ci_if_error: true

      - name: Install docs dependencies
        run: |
          python -m pip install --upgrade pip
          pip install breathe "myst-parser>=3.0"
          pip install sphinxcontrib.mermaid
          pip install sphinx-rtd-theme
          pip install pytest pytest-cov

      - name: Install Doxygen
        run: |
          sudo apt-get update
          sudo apt-get install -y doxygen

      - name: Run Doxygen (generate XML for Breathe)
        run: |
          rm -rf docs/_build docs/_autosummary
          mkdir -p docs/_build/doxygen
          doxygen docs/Doxyfile
          sphinx-build -b html docs docs/_build/html

      # C++
      - name: Configure
        run: cmake -S cpp -B build -DCMAKE_BUILD_TYPE=Release

      - name: Build
        run: | 
          cmake --build build --config Release -- -j 4

      - name: Run tests
        run: ctest --test-dir build --output-on-failure